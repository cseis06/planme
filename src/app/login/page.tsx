import { LoginForm } from "./login-form";

export default function LoginPage() {
  return (
    <main className="min-h-dvh w-full bg-cream flex items-center justify-center p-4">
      <LoginForm />
    </main>
  );
}